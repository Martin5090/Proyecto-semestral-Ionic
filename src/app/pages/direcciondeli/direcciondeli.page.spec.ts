import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DirecciondeliPage } from './direcciondeli.page';

describe('DirecciondeliPage', () => {
  let component: DirecciondeliPage;
  let fixture: ComponentFixture<DirecciondeliPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DirecciondeliPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
